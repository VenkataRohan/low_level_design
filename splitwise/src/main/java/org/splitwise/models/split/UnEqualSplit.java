package org.splitwise.models.split;
import org.splitwise.models.Split;

public class UnEqualSplit extends Split {
    public UnEqualSplit(String userId, int amount) {
        super(userId, amount);
    }
}